"""Pytest configuration.

Kept intentionally minimal. Its presence at the backend root ensures the
``app`` package is importable when running ``pytest`` from this directory
without an editable install.
"""
